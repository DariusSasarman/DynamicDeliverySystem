import React, { useState } from 'react';


function TransferPackage() {

    /// 1.Changes the "current" manager
    /// Pops the ex manager to a queue
    
    /// 2.Sets the status to "Pending"
    /// Such that the manager can send someone
    /// to pick it up.

    /// Managers show up in 
    /// Order of distance from
    /// The manager's station

    return ( <div> return </div>);
}

export default TransferPackage;